# Generated by confd {{datetime}}
<% @groups.each do |group| -%>
{{if exists "<%= group %>"}}{{range jsonArray (getv "<%= group %>")}}
-A INPUT -p {{.protocol}} -m {{.protocol}} -s {{.host}} --dport {{.port}} -j ACCEPT
{{end}}{{end}}
<% end -%>
