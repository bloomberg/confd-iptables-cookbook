# Generated by confd at {{dateTime}}
{{range gets "/groups/<%= node.ipaddress %>/*"}}
{{$data := json .Value}}
-A INPUT -p {{$data.protocol}} -m {{$data.protocol}} -s {{$data.host}} --dport {{$data.port}} -j ACCEPT
{{end}}
